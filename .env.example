# Puertodel servidor
PORT=3000

# Secreto para firma de JWT en panel de administración (Cambiar en producción)
JWT_SECRET=micasita_secret_key_super_segura_2026_mexican_food

# URL pública del sitio (para Open Graph, sitemap y URLs de código QR)
SITE_URL=http://localhost:3000

# Directorio de datos SQLite y fotos subidas (Recomendado fuera de carpetas sincronizadas en nube)
DATA_DIR=backend/data

# Entorno de ejecución (production activa HSTS, caché estática estricta)
NODE_ENV=development
